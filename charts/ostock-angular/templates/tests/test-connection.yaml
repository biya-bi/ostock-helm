apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ostock-angular.fullname" . }}-test-connection"
  labels:
    {{- include "ostock-angular.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ostock-angular.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

apiVersion: v1
kind: Service
metadata:
  name: {{ include "ostock-angular.fullname" . }}
  labels:
    {{- include "ostock-angular.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ostock-angular.selectorLabels" . | nindent 4 }}
